# Terraform Backend Configuration (S3)
# Uncomment and configure this to enable remote state management

# terraform {
#   backend "s3" {
#     bucket         = "your-terraform-state-bucket"
#     key            = "django-app/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "terraform-locks"
#   }
# }

# How to set up S3 remote backend:
#
# 1. Create S3 bucket:
#    aws s3api create-bucket --bucket your-terraform-state-bucket --region us-east-1
#
# 2. Enable versioning:
#    aws s3api put-bucket-versioning \
#      --bucket your-terraform-state-bucket \
#      --versioning-configuration Status=Enabled
#
# 3. Enable encryption:
#    aws s3api put-bucket-encryption \
#      --bucket your-terraform-state-bucket \
#      --server-side-encryption-configuration '...'
#
# 4. Block public access:
#    aws s3api put-public-access-block \
#      --bucket your-terraform-state-bucket \
#      --public-access-block-configuration \
#      "BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true"
#
# 5. Create DynamoDB table for locks:
#    aws dynamodb create-table \
#      --table-name terraform-locks \
#      --attribute-definitions AttributeName=LockID,AttributeType=S \
#      --key-schema AttributeName=LockID,KeyType=HASH \
#      --provisioned-throughput ReadCapacityUnits=5,WriteCapacityUnits=5
#
# 6. Update the backend configuration with your bucket name
#
# 7. Run: terraform init -migrate-state
